import { Heart } from "lucide-react"

export default function MentorPage() {
    return (
        <p className="flex gap-2 items-center absolute top-[50%] left-[50%] translate-x-[-50%] translate-y-[-50%] text-lg md:text-xl">На этой странице будет информация о наставнике... <Heart className="text-primary fill-primary" /></p>
    )
}